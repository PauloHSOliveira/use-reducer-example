import {CSSProperties} from 'react'

const button_styles:CSSProperties = {
    marginTop:'10px'
}

const input_styles:CSSProperties = {
    marginTop:'20px'
}

const form_styles:CSSProperties = {
    display:'flex', 
    flexDirection:'column', 
    alignItems:'center', 
    justifyContent:'center'
}

export {
    button_styles,
    input_styles,
    form_styles
}